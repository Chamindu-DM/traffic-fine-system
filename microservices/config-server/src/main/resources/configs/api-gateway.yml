server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins:
              - "http://localhost:5173"
              - "http://localhost:5174"
              - "${CORS_ALLOWED_ORIGINS:}"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowedHeaders:
              - "*"
            allowCredentials: true
      routes:
        - id: auth-service
          uri: lb://auth-service
          predicates:
            - Path=/api/auth/**
        - id: admin-categories
          uri: lb://fine-service
          predicates:
            - Path=/api/admin/categories
          filters:
            - RewritePath=/api/admin/categories, /api/fine-categories
        - id: admin-officers
          uri: lb://fine-service
          predicates:
            - Path=/api/admin/officers
          filters:
            - RewritePath=/api/admin/officers, /api/officers
        - id: fine-service
          uri: lb://fine-service
          predicates:
            - Path=/api/fines/**
        - id: fine-categories
          uri: lb://fine-service
          predicates:
            - Path=/api/fine-categories/**
        - id: payment-service
          uri: lb://payment-service
          predicates:
            - Path=/api/payments/**
        - id: reporting-service
          uri: lb://reporting-service
          predicates:
            - Path=/api/admin/**
        - id: auth-service-openapi
          uri: lb://auth-service
          predicates:
            - Path=/v3/api-docs/auth-service
          filters:
            - RewritePath=/v3/api-docs/auth-service, /v3/api-docs
        - id: fine-service-openapi
          uri: lb://fine-service
          predicates:
            - Path=/v3/api-docs/fine-service
          filters:
            - RewritePath=/v3/api-docs/fine-service, /v3/api-docs
        - id: payment-service-openapi
          uri: lb://payment-service
          predicates:
            - Path=/v3/api-docs/payment-service
          filters:
            - RewritePath=/v3/api-docs/payment-service, /v3/api-docs
        - id: reporting-service-openapi
          uri: lb://reporting-service
          predicates:
            - Path=/v3/api-docs/reporting-service
          filters:
            - RewritePath=/v3/api-docs/reporting-service, /v3/api-docs
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

app:
  jwt:
    secret: ${JWT_SECRET:traffic-fine-system-microservices-secret-key-change-in-production}
    public-paths:
      - /api/auth/login
      - /api/fines/lookup
      - /api/fine-categories/**
      - /api/payments
      - /api/payments/initiate
      - /api/payments/notify
      - /api/payments/simulate-webhook
      - /swagger-ui/**
      - /v3/api-docs/**
      - /actuator/**

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
  instance:
    prefer-ip-address: true

resilience4j:
  circuitbreaker:
    instances:
      paymentCircuitBreaker:
        sliding-window-size: 10
        failure-rate-threshold: 50
        wait-duration-in-open-state: 30s
        permitted-number-of-calls-in-half-open-state: 5
      fineCircuitBreaker:
        sliding-window-size: 10
        failure-rate-threshold: 50
        wait-duration-in-open-state: 30s
        permitted-number-of-calls-in-half-open-state: 5

management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway
  endpoint:
    health:
      show-details: always
    gateway:
      enabled: true

springdoc:
  swagger-ui:
    urls:
      - name: auth-service
        url: /v3/api-docs/auth-service
      - name: fine-service
        url: /v3/api-docs/fine-service
      - name: payment-service
        url: /v3/api-docs/payment-service
      - name: reporting-service
        url: /v3/api-docs/reporting-service
